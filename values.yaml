replicaCount: 1
image:
  repository: ghcr.io/fitnest-backend/payment-service
  tag: "development-8910bb6"
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-secret
service:
  type: ClusterIP
  port: 8080
istio:
  enabled: true
  host: "payment-service"
  mtls:
    mode: STRICT
  authorization:
    internalCallers:
      - "cluster.local/ns/fitnest-dev/sa/user-service"
      - "cluster.local/ns/fitnest-dev/sa/identity-service"
      - "cluster.local/ns/fitnest-dev/sa/order-service"
      - "cluster.local/ns/fitnest-dev/sa/api-gateway"
env:
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgresql-payment:5432/fitnest_payment"
  SPRING_DATASOURCE_USERNAME: "fitnest_payment"
  SPRING_DATASOURCE_PASSWORD: "payment_password_123"
  SPRING_JPA_HIBERNATE_DDL_AUTO: "update"
  SPRING_REDIS_HOST: "redis.fitnest-dev.svc.cluster.local"
  SPRING_REDIS_PORT: "6379"
  SPRING_REDIS_PASSWORD: ""
  KAFKA_BOOTSTRAP_SERVERS: "kafka-service.fitnest-dev.svc.cluster.local:9092"
  JWT_SECRET: "your-jwt-secret-change-in-production"
  JWT_ISSUER: "fitnest"
  IDENTITY_SERVICE_URL: "http://identity-service:8080"
  USER_SERVICE_URL: "http://user-service:8080"
  MEDIA_SERVICE_URL: "http://media-service:8086"
  identity-service: "cluster.local/ns/fitnest-dev/sa/identity-service"
  order-service: "cluster.local/ns/fitnest-dev/sa/order-service"
  api-gateway: "cluster.local/ns/fitnest-dev/sa/api-gateway"
livenessProbe:
  httpGet:
    path: /actuator/health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 15
readinessProbe:
  httpGet:
    path: /actuator/health
    port: http
  initialDelaySeconds: 20
  periodSeconds: 10
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
